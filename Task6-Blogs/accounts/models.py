from django.db import models

# Create your models here.
# we don't actually have to write a new model, django has a default user model 
# django.contrib.auth.models.User
# i can point my other models to this user model and it will work just fine